rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All access is mediated by the server (Firebase Admin SDK), which bypasses these rules.
    // No direct client (browser) read or write is permitted.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
